name: QA Daily Report

on:
  schedule:
    - cron: '0 4 * * *'   # יעד 07:00 שעון ישראל (UTC+3 קיץ) — מוקדם בשעה כדי לפצות על עיכובי GitHub Actions ולהגיע בפועל ~08:00
  workflow_dispatch:        # הפעלה ידנית מ-GitHub

jobs:
  run-qa-agent:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          for i in 1 2 3; do
            pip install -r requirements.txt twilio && break
            echo "Attempt $i failed, retrying in 10s..."
            sleep 10
          done

      - name: Install Playwright browsers with system dependencies
        run: python -m playwright install --with-deps chromium firefox webkit

      - name: Create config.py from secrets
        run: |
          cat > config.py << 'EOF'
          WHATSAPP_PHONE     = '${{ secrets.WHATSAPP_PHONE }}'
          TWILIO_ACCOUNT_SID = '${{ secrets.TWILIO_ACCOUNT_SID }}'
          TWILIO_AUTH_TOKEN  = '${{ secrets.TWILIO_AUTH_TOKEN }}'
          TWILIO_FROM_NUMBER = '${{ secrets.TWILIO_FROM_NUMBER }}'
          EOF

      - name: Run QA Agent
        run: python qa_agent.py
        env:
          PYTHONIOENCODING: utf-8

      - name: Upload QA Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: qa-report-${{ github.run_number }}
          path: reports/
          retention-days: 30
